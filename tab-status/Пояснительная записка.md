# Пояснительная записка к плагину "Статус Вкладки"

## 1. Общее описание

**Статус Вкладки** — это расширение для браузера Google Chrome, позволяющее добавлять эмодзи-статусы к заголовкам вкладок для лучшей организации рабочего процесса. Эмодзи отображается как замена фавиконки (иконки вкладки) и в начале текста заголовка, что обеспечивает визуальную идентификацию назначения вкладки.

**Основное назначение:**
- Визуальная категоризация вкладок по типу задачи.
- Быстрая идентификация приоритетных вкладок.
- Улучшение организации работы с множеством открытых вкладок.

**Целевая аудитория:**
- Пользователи, работающие с большим количеством вкладок одновременно.
- Специалисты, которым требуется сортировка вкладок по контексту (чтение, ответы, исследования и т.д.).
- Любой пользователь Chrome, желающий улучшить навигацию между вкладками.

## 2. Функциональные возможности

### 2.1. Основные функции
- **Контекстное меню:** Пункт "Статус" с подменю появляется при клике правой кнопкой мыши на странице.
- **Набор статусов:** Пять предопределенных статусов с эмодзи:
  - 📖 Прочитать
  - ↩️ Ответить
  - 🔍 Исследовать
  - 🔥 Сделать / Срочно
  - ℹ️ Справка
- **Очистка статуса:** Опция удаления эмодзи из заголовка и восстановления оригинальной фавиконки.
- **Toggle-логика:** Повторный выбор того же статуса снимает его (переключатель).
- **Визуальное отображение:**
  - Эмодзи добавляется в начало заголовка вкладки.
  - Фавиконка заменяется на сгенерированное изображение с эмодзи.
- **Сохранение состояния:** Статус сохраняется при перезагрузке страницы (через модификацию заголовка).

### 2.2. Дополнительные возможности
- **Автоматическое восстановление фавиконки:** Оригинальная фавиконка сохраняется и может быть восстановлена при очистке статуса.
- **Поддержка всех страниц:** Работает на любых веб-страницах (включая локальные файлы и расширенные URL).
- **Устойчивость к дублированию:** Очистка заголовка от предыдущих эмодзи перед установкой нового.

## 3. Архитектура

### 3.1. Контекстная диаграмма (C4 Level 1)
![Контекстная диаграмма](c4_Level_1_context_diagram_TabStatus_v1.plantuml)

**Участники:**
- **Пользователь** — инициатор действий, выбирает статус из контекстного меню.
- **Статус Вкладки** — расширение Chrome, обрабатывающее выбор статуса.
- **Браузер Chrome** — платформа, предоставляющая API расширений и интерфейс вкладок.
- **Chrome Tabs API** — API для управления свойствами вкладок (заголовок, иконка).

**Взаимодействия:**
1. Пользователь кликает правой кнопкой мыши на странице и выбирает статус из меню.
2. Расширение получает событие через Chrome Context Menus API.
3. Расширение модифицирует заголовок вкладки и фавиконку через Chrome Tabs API и Scripting API.
4. Изменения отображаются в интерфейсе браузера.

### 3.2. Диаграмма контейнеров (C4 Level 2)
![Диаграмма контейнеров](c4_Level_2_containers_diagram_TabStatus_v1.plantuml)

**Компоненты расширения:**
1. **Service Worker (background.js)** — фоновый скрипт, отвечающий за:
   - Создание иерархического контекстного меню при установке.
   - Обработку кликов по пунктам меню.
   - Выполнение скрипта на активной вкладке для изменения заголовка и фавиконки.
2. **Состояние вкладок (chrome.storage)** — хранилище для сохранения статусов вкладок (в текущей реализации не используется, состояние хранится в заголовке).
3. **Вкладка Chrome** — целевой объект модификации:
   - Заголовок (document.title).
   - Фавиконка (link[rel="icon"]).

**Взаимодействие компонентов:**
- Service Worker создает контекстное меню с пунктами статусов.
- При клике Service Worker выполняет скрипт на активной вкладке с помощью `chrome.scripting.executeScript`.
- Скрипт модифицирует DOM страницы (заголовок и фавиконку).
- Изменения мгновенно отображаются в интерфейсе браузера.

## 4. Техническая реализация

### 4.1. Манифест (manifest.json)
- **Manifest V3** — современный стандарт расширений Chrome.
- **Права доступа:**
  - `contextMenus` — для создания контекстного меню.
  - `scripting` — для выполнения скриптов на вкладках.
- **Разрешения хостов:** `<all_urls>` для работы на всех страницах.
- **Структура:**
  - `background.service_worker` — указание на background.js.
  - Отсутствие content_scripts — скрипт внедряется динамически.

### 4.2. Ключевые модули

#### 4.2.1. background.js
- **Инициализация:** Создание родительского пункта "Статус" и дочерних пунктов для каждого статуса при установке расширения.
- **Обработка кликов:** Прослушивание события `chrome.contextMenus.onClicked`:
  - Определение выбранного статуса.
  - Подготовка списка всех эмодзи для очистки.
  - Вызов функции `setTabStatus` на активной вкладке через `chrome.scripting.executeScript`.

#### 4.2.2. Функция setTabStatus (выполняется на странице)
- **Проверка toggle:** Определяет, был ли уже установлен тот же эмодзи в начале заголовка. Если да — интерпретирует действие как снятие статуса.
- **Очистка заголовка:** Удаляет все известные эмодзи из начала заголовка с помощью регулярного выражения.
- **Установка нового заголовка:**
  - Если действие toggle или очистка — устанавливается очищенный заголовок.
  - Иначе — добавляется эмодзи и пробел перед очищенным заголовком.
- **Обновление фавиконки:**
  - Сохраняет оригинальную фавиконку в `document.head.dataset.originalFavicon`.
  - При установке статуса генерирует data URL с изображением эмодзи на canvas и устанавливает как href ссылки с `rel="icon"`.
  - При очистке восстанавливает оригинальную фавиконку из dataset.

### 4.3. Алгоритмы и структуры данных

#### 4.3.1. Структура статусов
```javascript
const statuses = {
  "read": { title: "Прочитать", emoji: "📖" },
  "reply": { title: "Ответить", emoji: "↩️" },
  "research": { title: "Исследовать", emoji: "🔍" },
  "todo": { title: "Сделать / Срочно", emoji: "🔥" },
  "reference": { title: "Справка", emoji: "ℹ️" },
  "clear": { title: "Очистить статус", emoji: "" }
};
```

#### 4.3.2. Очистка заголовка от эмодзи
Используется регулярное выражение, построенное из всех эмодзи статусов:
```javascript
const regexPattern = new RegExp(`^(${allEmojisList.join('|')})\\s*`, 'u');
```
Выражение ищет любой из эмодзи в начале строки, за которым следует любое количество пробелов. Цикл `while` удаляет все повторяющиеся эмодзи.

#### 4.3.3. Генерация фавиконки
1. Создается элемент `canvas` размером 64x64 пикселей.
2. На canvas рисуется эмодзи с использованием шрифта serif размером 54px.
3. Canvas конвертируется в data URL формата `image/x-icon`.
4. Ссылка с `rel="icon"` обновляется этим data URL.

#### 4.3.4. Сохранение оригинальной фавиконки
Оригинальная фавиконка сохраняется в `document.head.dataset.originalFavicon` при первом изменении. Это позволяет восстановить её позже.

### 4.4. Обработка ошибок и крайние случаи
- **Отсутствие фавиконки:** Если на странице нет элемента `<link rel="icon">`, он создается.
- **Data URL как оригинальная фавиконка:** Если текущая фавиконка уже является data URL (сгенерированной ранее), она не сохраняется как оригинал.
- **Множественные эмодзи в заголовке:** Регулярное выражение удаляет все эмодзи из начала заголовка, даже если их несколько.
- **Unicode-безопасность:** Использование флага `u` в регулярном выражении обеспечивает корректную работу с эмодзи.

## 5. Хранение данных

### 5.1. Состояние
В текущей реализации состояние не сохраняется в отдельном хранилище. Информация о статусе вкладки хранится непосредственно в заголовке страницы (добавленный эмодзи) и в dataset для фавиконки. Это обеспечивает простоту и отсутствие необходимости синхронизации, но имеет ограничения:
- При закрытии вкладки состояние теряется (если страница не сохраняет заголовок).
- Не поддерживается перенос статуса между сессиями браузера (если страница перезагружается, заголовок сохраняется, но фавиконка может сброситься).

### 5.2. Возможности расширения хранилища
Для полноценного сохранения состояния между сессиями можно использовать `chrome.storage.local` или `chrome.storage.session`. Ключевые данные для сохранения:
- URL вкладки
- Установленный статус
- Оригинальная фавиконка (в виде data URL)

## 6. Взаимодействие с браузером

### 6.1. Chrome Extensions API
- **contextMenus:** Создание иерархического меню с родительским и дочерними пунктами.
- **scripting:** Динамическое выполнение кода на активной вкладке (`executeScript`).
- **runtime:** Обработка событий установки и сообщений.

### 6.2. DOM Manipulation
Скрипт, выполняемый на странице, модифицирует:
- `document.title` — для отображения эмодзи в заголовке вкладки.
- Элемент `<link rel="icon">` — для замены фавиконки.
- `document.head.dataset` — для хранения оригинальной фавиконки.

### 6.3. Обработка событий
- **События расширения:** `chrome.runtime.onInstalled`, `chrome.contextMenus.onClicked`.
- **События страницы:** Не используются, так как скрипт выполняется однократно при выборе статуса.

## 7. Ограничения и известные проблемы

### 7.1. Ограничения
1. **Только активная вкладка:** Статус устанавливается только для вкладки, в которой было вызвано контекстное меню.
2. **Зависимость от заголовка:** Если страница динамически меняет заголовок (например, SPA), добавленный эмодзи может быть потерян.
3. **Размер фавиконки:** Canvas фиксированного размера 64x64, что может приводить к пикселизации на экранах с высоким DPI.
4. **Шрифт эмодзи:** Используется шрифт serif, который может отображать эмодзи неидеально на некоторых платформах.
5. **Безопасность:** Не работает на страницах с ограниченными политиками (chrome://, about:, расширения Chrome Web Store).

### 7.2. Известные проблемы
1. **Фавиконка на некоторых сайтах:** Некоторые сайты используют сложные механизмы установки фавиконки (несколько `<link>` элементов, SVG, JavaScript), что может приводить к некорректному восстановлению оригинальной иконки.
2. **Конфликты с другими расширениями:** Другие расширения, изменяющие заголовок или фавиконку, могут конфликтовать.
3. **Производительность:** Генерация data URL на canvas для каждой вкладки может незначительно влиять на производительность при частом использовании.

## 8. Рекомендации по развитию

### 8.1. Ближайшие улучшения
1. **Сохранение состояния:** Использование `chrome.storage` для запоминания статусов вкладок между сессиями.
2. **Настройка статусов:** Позволить пользователю добавлять собственные эмодзи и названия статусов.
3. **Горячие клавиши:** Назначение сочетаний клавиш для быстрой установки статусов.
4. **Панель управления:** Добавление popup-окна с кнопками статусов для управления без контекстного меню.
5. **Синхронизация:** Использование `chrome.storage.sync` для синхронизации статусов между устройствами.

### 8.2. Долгосрочные планы
1. **Группировка вкладок по статусам:** Возможность фильтрации и группировки вкладок на основе статусов.
2. **Интеграция с таск-трекерами:** Экспорт списка вкладок со статусами в формате, пригодном для импорта в системы управления задачами.
3. **Статистика:** Анализ времени, проведенного на вкладках с определенными статусами.
4. **Поддержка других браузеров:** Адаптация для Firefox, Edge, Safari.

## 9. Заключение

Плагин "Статус Вкладки" представляет собой простое и эффективное решение для визуальной организации вкладок в браузере Chrome. Использование эмодзи в заголовке и фавиконке позволяет быстро идентифицировать назначение вкладки, что повышает продуктивность при работе с большим количеством открытых страниц.

Расширение построено на современных технологиях (Manifest V3, Chrome Scripting API) и демонстрирует хороший пример использования возможностей браузера для модификации пользовательского интерфейса. Открытый исходный код и модульная архитектура позволяют легко дорабатывать и адаптировать плагин под конкретные нужды пользователей.

---
*Документация составлена на основе анализа исходного кода версии 1.1. Последнее обновление: 15.12.2025.*